[pytest]
testpaths = tests
# Rend le paquet "app" importable depuis backend/ sans dependre d'un PYTHONPATH
# exterieur : pytest n'ajoute que le repertoire des tests a sys.path.
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
